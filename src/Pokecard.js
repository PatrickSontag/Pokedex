import React from "react";

const pokemonAPI = "https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/";

function Pokecard({id}) {

    let pokemonImage = `${pokemonAPI}${prop.id}.png`

    return (
        <div>
            
        </div>
    );
}

export default Pokecard;